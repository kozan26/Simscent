{
  "name": "simscent-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html perfumes_data.json assets _redirects dist/ 2>/dev/null || true",
    "preview": "npx serve dist -l 4173"
  }
}
